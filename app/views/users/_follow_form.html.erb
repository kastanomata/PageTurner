<% unless @user.id == Current.user&.id %>
  <div style="align-items: center">
  <% if Current.user&.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>