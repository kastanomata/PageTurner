

<% content_for :title, "Users" %>

<h1>Users</h1>

<div class="cards-container" id="users">
  <% @users.each do |user| %>
    <%= render partial: "user_card", locals: { user: user } %>
  <% end %>
</div>
