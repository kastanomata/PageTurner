      <% if authenticated? %>
        <div style="margin-left: 20px;">
          <%= button_to "Generate Report", 
            reports_path(
              report: {
                reported_id: reported_id,
                reporter_id: reporter_id,
              }
            ), 
            method: :post, 
            class: "btn-link" %>
        </div>
      <% end %>