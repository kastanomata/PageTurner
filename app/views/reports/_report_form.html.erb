<% if authenticated? %>
  <div>
    <%= button_to "Generate Report", 
      reports_path(
        report: {
          reported_id: reported_id,
          reporter_id: reporter_id,
          reported_type: type
        }
      ), 
      method: :post, 
      class: "btn-link-red" %>
  </div>
<% end %>