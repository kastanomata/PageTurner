<%= button_to "Report #{reported.class.name}", reports_path,
  params: { 
    report: { 
      reporter_id: Current.user.id,
      reported_id: reported.id,
      reported_type: reported.class.name 
    }
  },
  data: { turbo: true },
  class: "btn btn--negative btn--sm",
  id: "report-button-#{dom_id(reported)}"  # Safer DOM ID generation
%>