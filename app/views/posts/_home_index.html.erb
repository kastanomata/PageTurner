<h2>Post Recenti</h2>
<% if @posts.any? %>
    <%
        # FIXME
        posts = Current.user.get_homepage_posts
    %>
    <ul>
        <%= render partial: 'posts/postcard', collection: posts, as: :post %>
    </ul>
<% else %>
    <p>Nessun post disponibile.</p>
<% end %>