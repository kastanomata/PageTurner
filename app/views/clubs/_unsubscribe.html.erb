<%= form_for(Current.user&.active_memberships.find_by(club_id: @club.id), html: { method: :delete }, remote: true) do |f| %>
    <div><%= hidden_field_tag :club_id, @club.id %></div>
    <%= f.submit "Unsubscribe", class: "btn" %>
<% end %>